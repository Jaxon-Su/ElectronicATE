# ElectronicATE v0.2.0-alpha.3

此版本整合架構重構，提供 Windows x64 預覽套件。

## 主要變更

- Page1／Page2／Page3 的可測試核心模組與硬體工廠分離。
- Page1 設定完整寫入後再通知；處理重入載入及頁面關閉。
- Page2 表格一次提交條件快照，Page3 使用完整快照；補上無視窗表格測試。
- Page3 手動操作依序執行，擷取與未接手的示波器連線有明確釋放流程。
- Page4 連線、指令及斷線移至同一專用背景執行緒；介面不再執行阻塞通訊，取消後丟棄舊結果。
- Page3／Page4／Page5 控制期間互斥：Page3 指令、擷取及輸出 ON、Page4 連線、Page5 測試執行時停用其他控制頁。
- Page3 關閉輸出成功後解鎖；失敗保留鎖定並恢復按鈕供重試。Page4 斷線仍等待當前指令返回才解鎖。
- XML 整份預先驗證，套用期間抑制頁面通知；失敗回復原設定，成功同步頁面依賴後再通知。拒絕重複頁面段落與巢狀載入。
- AC Source／Load 關閉後讀回 OFF，Relay 確認開路；未知或失敗回應不解鎖。ON 指令失敗也保留控制權，供 OFF 重試。
- 控制期間鎖住設定頁與檔案選單；關閉視窗先停止測試或中止通訊，Page3 須先確認輸出已關閉。
- 二進位檔案保留原子寫入。
- AC Source／Load／Relay 統一優先使用有效的結構化通訊地址。

## 驗證

- 26 組 CTest 測試通過，包含 offscreen QtWidgets 測試。
- 使用 Qt 6.12.0、MinGW GCC 13.1.0 建置 Windows x64 Release。
- 未進行實體儀器測試。

## 安裝

解壓縮 win64.zip，執行 ElectronicATE.exe。套件包含 Qt 與 MinGW 執行階段及 XML 設定。
啟動程式前需另行安裝相容的 64 位元 NI-VISA 執行階段（執行檔直接相依 VISA64.dll）；儀器可能另需其驅動。本套件不包含 NI-VISA 或儀器驅動。
覆蓋舊版本前請自行備份既有設定。SHA256SUMS.txt 可用來驗證下載檔案。

## 已知限制

- 控制頁互斥不是跨程式或跨電腦的儀器資源鎖。OFF 讀回確認為儀器邏輯狀態，不代表端子殘留電壓已降至安全值。
- Page4 的取消不會強制終止驅動函式；背景讀取／開啟返回後才關閉連線並解鎖。
- AC Source 使用 OUTPut?、Load 使用 LOAD?；型號／韌體不支援或回傳格式不符時，會保留鎖定並顯示讀回失敗，需要實機確認。
- XML 回復涵蓋已序列化設定；新增欄位仍需加入序列化與驗證。
- 實機重連、關閉與驅動執行緒相容性尚待驗證；驗收步驟見 docs/hardware-acceptance.md。

此版本由 refactor/architecture-alpha3 分支發布，保留 main 作為原先基準。
