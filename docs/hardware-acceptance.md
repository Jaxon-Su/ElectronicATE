# 實機驗收：尚未執行

離線 CTest 已驗證通訊取消、背景工作生命週期、控制頁鎖定、OFF 回應解析及 XML 失敗回復。本表不表示實機通過。

開始前記錄型號、韌體、地址、測試負載、接線及緊急斷電方式。由現場人員確認後執行，不自動啟動未知負載。

| 項目 | 操作及預期 | 實測 |
|---|---|---|
| Page3 輸出 | ON 後其他控制頁與設定頁停用；OFF 讀回確認後解鎖 | 待驗證 |
| OFF 失敗 | 中斷通訊後 OFF 應報錯且保留控制權；恢復連線後可重試 | 待驗證 |
| 多通道負載／Relay | 確認每一已設定通道關閉；Relay 回應的位址、功能碼及 CRC 正確 | 待驗證 |
| Page4 慢速查詢 | 介面仍可操作；按斷線後等待驅動返回，拒絕舊回應，再解鎖 | 待驗證 |
| Page5 停止 | 測試途中停止，直到 worker 完成才解鎖；切頁後無殘留命令 | 待驗證 |
| 關閉視窗 | 控制中不直接銷毀資源；先停止／斷線，Page3 手動 OFF，完成後再關閉 | 待驗證 |
| XML 設定 | 無控制時載入完整設定；損壞或重複段落失敗後原設定維持一致 | 待驗證 |

AC Source 使用 `OUTPut?`，DC Load 使用 `LOAD?`；只接受明確 OFF／數值零，明確 ON 最多重試三次，其餘回應報錯。各型號與韌體須現場確認支援。這是邏輯狀態確認，不是電壓安全量測。
